// A é Igual a B
let  A =  10;
let B = 20;
let equal = A === B;

// A é Diferente de B
let different = A !== B;

// A é Maior que B
let bigger = A > B;

// A é Menor que B
let smaller = A < B;

// A é Maior ou Igual a B
let biggerOrEqual = A >= B;

// A é Menor ou Igual a B
let lessOrEqual = A <= B;


// Calcular Nota
let nota1 = parseFloat(prompt("Digite a Primeira Nota"));
let nota2 = parseFloat(prompt("Digite a Segunda Nota"));
let media = (nota1 + nota2) / 2;
alert(media >=7);